const initialState = {
  type: '',
  coordinates: []
};

const activeTetrominoReducer = (
  state = initialState,
  action
) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default activeTetrominoReducer;
